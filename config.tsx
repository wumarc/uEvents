// This file is intended for configuration
// This is shared configuration. For personal config, use userConfig.tsx

// App version
// Make sure to document every version in the changelog.md file !!!
// version 1.2.3
// 1: major version
// 2: minor version
// 3: patch version
// Major version update will force the user to update.
// Minor version update will not force an update but includes a message with update info
// Patch version update will not force an update and will not include a message
// Every time you create a new version, make sure to update both this file and the database in "versions"
export const appVersion = "1.0.0";
export const appVersionMajor = 1;
export const appVersionMinor = 0;
export const appVersionPatch = 0;
