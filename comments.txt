const addEvent = async () => {
  
  await setDoc(doc(db, "events", uuidv4()), {
    date: "2023-04-15",    
    time: "6",    
    location: "Central Park, New York City",    
    event_name: "Basketball 5v5",    
    event_description: "Join us for an evening of live music in the beautiful Central Park. We'll have a variety of musicians playing different genres, from classical to jazz to rock.",    "event_organizer_name": "Central Park Conservancy",   
    num_attendees: 150
  });
}

    setWebLinks(await Promise.all(treatmentIds.map(async (id) => {
      let data = await getRequest(`/learn/${id}`);
      console.log(webLinks)
      console.log(data)
      return data;
    })))

    
    // treatmentIds.forEach(async (id) => {
    //   let data = await getRequest(`/learn/${id}`);
    //   console.log(webLinks)
    //   console.log(data)
    //   if (data.length) setWebLinks([...webLinks, data]);
    // });