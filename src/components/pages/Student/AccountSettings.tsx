import { View, Text } from "react-native";

const AccountSettings = () => {

    return (
        <View>
            <Text>
                Account Settings
            </Text>
        </View>
    );

};

export default AccountSettings;